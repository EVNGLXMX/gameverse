import { createTheme } from "@mui/material";
const theme = createTheme({   
    palette: {
        mode: "dark",
        primary:{
            main: '#'
        }
    },
    typography:{
        gameverse:{
            color:'#4fc3f7',
            fontSize:"1.5rem"
        }
    },
    cardMedia:{
        gamecard:{
            
        }
    }
});

export default theme;